import React from 'react';
import './Navbar.css';

const Navbar = () => {
    return (
        <nav className="navbar">
            <h2>Election Commission Portal</h2>
        </nav>
    );
};

export default Navbar;
